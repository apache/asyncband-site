import{A as e,E as t,F as n,K as r,L as i,M as a,R as o,U as s,X as c,Y as l,_ as u,a as d,b as f,d as p,f as m,g as h,h as g,i as _,m as v,o as y,q as b,u as x,v as S,x as C,y as w,z as T}from"../chunks/CxoQY3RK.js";import{a as E}from"../chunks/BNdqc5Z9.js";import"../chunks/xihTtKlq.js";import{t as D}from"../chunks/KVW5nrcb.js";var O=C(`<aside class="docs-toc" aria-label="On this page"><!></aside>`),k=C(`<div class="docs-layout"><aside class="docs-sidebar"><nav aria-label="Documentation sidebar"><a>About</a> <p class="sidebar-heading">Reference</p> <a class="sidebar-item external" href="https://docs.rs/asyncband/">API documentation</a></nav></aside> <main id="main-content" class="docs-main"><div class="docs-article-column"><nav class="breadcrumbs" aria-label="Breadcrumb"><a aria-label="Documentation home">⌂</a> <span aria-hidden="true">›</span> <span> </span></nav> <details class="mobile-doc-navigation"><summary>Documentation</summary> <nav><a>About</a></nav></details> <article class="doc-article" data-pagefind-body=""><!></article> <div class="doc-source"><a rel="external">Edit this page on GitHub</a></div></div> <!></main></div>`);function A(e,n){b(n,!0);let o=_(n,`label`,3,`About`),d=s(()=>`https://github.com/apache/asyncband-site/edit/main/src/routes${D.url.pathname}+page.md`);var f=k(),m=i(f),h=i(m),g=i(h);let y;l(4),c(h),c(m);var C=T(m,2),A=i(C),j=i(A),M=i(j),N=T(M,4),P=i(N,!0);c(N),c(j);var F=T(j,2),I=T(i(F),2),L=i(I);c(I),c(F);var R=T(F,2),z=i(R);v(z,()=>n.children),c(R);var B=T(R,2),V=i(B);c(B),c(A);var H=T(A,2),U=e=>{var t=O(),r=i(t);v(r,()=>n.toc),c(t),w(e,t)};u(H,e=>{n.toc&&e(U)}),c(C),c(f),a((e,n,r,i)=>{y=p(g,1,`sidebar-item`,null,y,e),x(g,`href`,n),x(M,`href`,r),S(P,o()),x(L,`href`,i),x(V,`href`,t(d))},[()=>({active:D.url.pathname===E(`/docs/about/`)}),()=>E(`/docs/about/`),()=>E(`/docs/about/`),()=>E(`/docs/about/`)]),w(e,f),r()}var j=C(`<meta name="description"/> <link rel="canonical"/> <meta property="og:url"/> <meta property="og:title"/> <meta property="og:description"/>`,1),M=C(`<li><a> </a></li>`),N=C(`<ul></ul>`);function P(r,s){let l=_(s,`ogTitle`,19,()=>s.title),u=_(s,`toc`,19,()=>[]);m(`t31tvm`,t=>{var r=j(),i=o(r),c=T(i,2),u=T(c,2),d=T(u,2),f=T(d,2);a(()=>{x(i,`content`,s.description),x(c,`href`,s.canonical),x(u,`content`,s.canonical),x(d,`content`,l()),x(f,`content`,s.description)}),e(()=>{n.title=s.title??``}),w(t,r)}),A(r,{get label(){return s.label},toc:e=>{var n=N();h(n,21,u,e=>e.id,(e,n)=>{var r=M(),o=i(r),s=i(o,!0);c(o),c(r),a(()=>{x(o,`href`,`#${t(n).id??``}`),S(s,t(n).label)}),w(e,r)}),c(n),w(e,n)},children:(e,t)=>{var n=f(),r=o(n);v(r,()=>s.children),w(e,n)},$$slots:{toc:!0,default:!0}})}var F={label:`About`,title:`Welcome to Apache Asyncband | Apache Asyncband`,description:`Apache Asyncband is a runtime-agnostic library providing synchronization and coordination tools for asynchronous Rust programming.`,canonical:`https://asyncband.apache.org/docs/about/`,ogTitle:`Welcome to Apache Asyncband (Incubating)`,toc:[{id:`overview`,label:`Overview`},{id:`getting-started`,label:`Getting started`},{id:`api-map`,label:`API map`},{id:`synchronous-interoperability`,label:`Synchronous interoperability`},{id:`thread-safety`,label:`Thread safety`},{id:`minimum-supported-rust-version-msrv`,label:`Minimum Supported Rust Version (MSRV)`},{id:`license-and-trademarks`,label:`License and Trademarks`}]},{label:I,title:L,description:R,canonical:z,ogTitle:B,toc:V}=F,H=new Set([`$$slots`,`$$events`,`$$legacy`]),U=C(`<h1>Welcome to Apache Asyncband (Incubating)</h1> <h2 id="overview">Overview</h2> <p>Asyncband is a focused collection of composable, runtime-agnostic concurrency building blocks for async Rust. It provides synchronization, initialization, task coordination, channels, resource reuse, and workload control without choosing an executor for the application.</p> <p>Asyncband's async APIs are built on standard futures and wakers. The library does not spawn tasks, own worker threads, install timers, or require a reactor or I/O driver. Applications can poll its futures with Tokio, async-std, smol, a custom executor, or any other standards-based runtime, and compose runtime services such as deadlines around them.</p> <h3>Project scope</h3> <p>The project is not limited to small or stateless synchronization primitives. Stateful utilities such as a singleflight group or an object pool fit when they provide a generally reusable coordination mechanism and remain independent of executor policy.</p> <p>The boundary is mechanism versus policy. Task placement, timers, deadlines, retries, periodic maintenance, and application lifecycle orchestration stay with the caller and its runtime. Potential future-concurrency or scheduling APIs are evaluated against the same boundary: they must remain executor-independent and compose with caller-owned execution and timing.</p> <h2 id="getting-started">Getting started</h2> <p>The crate enables no APIs by default. Enable only the features your application uses:</p> <pre data-language="bash"><code class="hljs language-bash"></code></pre> <pre data-language="rust"><code class="hljs language-rust"></code></pre> <p>Public paths stay direct—such as <code>asyncband::mutex</code>, <code>asyncband::pool</code>, and <code>asyncband::once::OnceCell</code>—while Cargo features keep unused implementations out of the build.</p> <h2 id="api-map">API map</h2> <div class="table-wrapper"><table><thead><tr><th>Area</th><th>API</th><th>Feature</th><th>Use</th></tr></thead><tbody><tr><td>Shared state</td><td><a href="https://docs.rs/asyncband/*/asyncband/mutex/struct.Mutex.html" rel="nofollow"><code>Mutex</code></a></td><td><code>mutex</code></td><td>Protect shared data with asynchronous mutual exclusion.</td></tr><tr><td></td><td><a href="https://docs.rs/asyncband/*/asyncband/rwlock/struct.RwLock.html" rel="nofollow"><code>RwLock</code></a></td><td><code>rwlock</code></td><td>Allow multiple readers or one writer.</td></tr><tr><td></td><td><a href="https://docs.rs/asyncband/*/asyncband/condvar/struct.Condvar.html" rel="nofollow"><code>Condvar</code></a></td><td><code>condvar</code></td><td>Wait for notifications while releasing a mutex.</td></tr><tr><td>Initialization</td><td><a href="https://docs.rs/asyncband/*/asyncband/once/struct.Once.html" rel="nofollow"><code>Once</code></a></td><td><code>once</code></td><td>Run asynchronous initialization exactly once.</td></tr><tr><td></td><td><a href="https://docs.rs/asyncband/*/asyncband/once/struct.OnceCell.html" rel="nofollow"><code>OnceCell</code></a></td><td><code>once-cell</code></td><td>Initialize and store one asynchronous value.</td></tr><tr><td></td><td><a href="https://docs.rs/asyncband/*/asyncband/once/struct.LazyCell.html" rel="nofollow"><code>LazyCell</code></a></td><td><code>once-cell</code></td><td>Lazily initialize a value with a stored asynchronous function.</td></tr><tr><td></td><td><a href="https://docs.rs/asyncband/*/asyncband/once/struct.OnceMap.html" rel="nofollow"><code>OnceMap</code></a></td><td><code>once-map</code></td><td>Initialize and store one value per key.</td></tr><tr><td>Task coordination</td><td><a href="https://docs.rs/asyncband/*/asyncband/barrier/struct.Barrier.html" rel="nofollow"><code>Barrier</code></a></td><td><code>barrier</code></td><td>Wait until all participants reach a synchronization point.</td></tr><tr><td></td><td><a href="https://docs.rs/asyncband/*/asyncband/latch/struct.Latch.html" rel="nofollow"><code>Latch</code></a></td><td><code>latch</code></td><td>Wait until a one-way countdown completes.</td></tr><tr><td></td><td><a href="https://docs.rs/asyncband/*/asyncband/waitgroup/struct.WaitGroup.html" rel="nofollow"><code>WaitGroup</code></a></td><td><code>waitgroup</code></td><td>Wait for a dynamic group of tasks to finish.</td></tr><tr><td></td><td><a href="https://docs.rs/asyncband/*/asyncband/shutdown/struct.Shutdown.html" rel="nofollow"><code>Shutdown</code></a></td><td><code>shutdown</code></td><td>Coordinate shutdown signals and completion.</td></tr><tr><td>Channels</td><td><a href="https://docs.rs/asyncband/*/asyncband/oneshot/" rel="nofollow"><code>oneshot</code></a></td><td><code>oneshot</code></td><td>Send one value between two tasks.</td></tr><tr><td></td><td><a href="https://docs.rs/asyncband/*/asyncband/mpsc/" rel="nofollow"><code>mpsc</code></a></td><td><code>mpsc</code></td><td>Send each value from multiple producers to one receiver.</td></tr><tr><td></td><td><a href="https://docs.rs/asyncband/*/asyncband/broadcast/" rel="nofollow"><code>broadcast</code></a></td><td><code>broadcast</code></td><td>Broadcast values from one or more producers and retain them until every active receiver consumes them.</td></tr><tr><td>Resource reuse</td><td><a href="https://docs.rs/asyncband/*/asyncband/pool/" rel="nofollow"><code>pool</code></a></td><td><code>pool</code></td><td>Reuse objects through bounded or unbounded pool variants.</td></tr><tr><td>Workload coordination</td><td><a href="https://docs.rs/asyncband/*/asyncband/semaphore/struct.Semaphore.html" rel="nofollow"><code>Semaphore</code></a></td><td><code>semaphore</code></td><td>Control concurrent access with permits.</td></tr><tr><td></td><td><a href="https://docs.rs/asyncband/*/asyncband/singleflight/struct.Group.html" rel="nofollow"><code>Group</code></a></td><td><code>singleflight</code></td><td>Coalesce concurrent calls for the same key.</td></tr><tr><td>Sync interop</td><td><a href="https://docs.rs/asyncband/*/asyncband/blocking/trait.FutureExt.html" rel="nofollow"><code>FutureExt</code></a></td><td><code>blocking</code></td><td>Drive one runtime-agnostic future from a blocking thread.</td></tr></tbody></table></div> <h2 id="synchronous-interoperability">Synchronous interoperability</h2> <p>The optional <code>blocking</code> module is a boundary adapter for synchronous callers. It parks the calling thread while driving one future; it is not a general-purpose executor.</p> <pre data-language="bash"><code class="hljs language-bash"></code></pre> <pre data-language="rust"><code class="hljs language-rust"></code></pre> <h3>Async first, blocking by adaptation</h3> <p>Async and synchronous synchronization primitives have different optimization constraints. Once an async operation is exposed as a runtime-agnostic future, synchronous code can usually drive that future through a <code>block_on</code> adapter. Asyncband therefore designs its primitives for async use and provides blocking interoperability at the boundary instead of duplicating synchronous methods across every type.</p> <p>A sync-first implementation can exploit OS- or platform-specific facilities that an async implementation cannot assume. Libraries focused on synchronous code can therefore make different and sometimes better tradeoffs. Asyncband leaves those optimizations to dedicated libraries rather than treating blocking adaptation as a second family of primitives.</p> <h3>Execution constraints</h3> <p>The <code>blocking</code> module is a lightweight, thread-parking single-future executor, not a general-purpose async runtime. <code>wait_timeout</code> drops the future on timeout. Futures that depend on a runtime-specific timer or I/O driver still need that runtime's driver to make progress, and blocking an executor thread can cause starvation or deadlocks. See the <a href="https://docs.rs/asyncband/*/asyncband/blocking/" rel="nofollow"><code>blocking</code> module documentation</a> for the full contract.</p> <h2 id="thread-safety">Thread safety</h2> <p>Asyncband types implement <code>Send</code> and <code>Sync</code> only when the protected, transferred, or managed value satisfies the necessary bounds. See each API's documentation for its exact contract.</p> <h2 id="minimum-supported-rust-version-msrv">Minimum Supported Rust Version (MSRV)</h2> <p>This crate is built against the latest stable release, and its minimum supported rustc version is 1.86.0.</p> <p>The policy is that the minimum Rust version required to use this crate can be increased in minor version updates. For example, if Asyncband 1.0 requires Rust 1.20.0, then Asyncband 1.0.z for all values of z will also require Rust 1.20.0 or newer. However, Asyncband 1.y for y > 0 may require a newer minimum version.</p> <h2 id="license-and-trademarks">License and Trademarks</h2> <p>This project is licensed under the <a href="https://www.apache.org/licenses/LICENSE-2.0" rel="nofollow">Apache License, Version 2.0</a>.</p> <p>Apache Asyncband, Asyncband, and Apache are either registered trademarks or trademarks of The Apache Software Foundation in the United States and/or other countries.</p>`,1);function W(e,t){let n=d(t,H);P(e,y(()=>n,()=>F,{children:(e,t)=>{var n=U(),r=T(o(n),18),a=i(r);g(a,()=>`cargo add asyncband --features mutex`,!0),c(a),c(r);var s=T(r,2),u=i(s);g(u,()=>`<span class="hljs-keyword">use</span> asyncband::mutex::Mutex;

<span class="hljs-keyword">async</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">increment</span>() &#123;
    <span class="hljs-keyword">let</span> <span class="hljs-variable">counter</span> = Mutex::<span class="hljs-title function_ invoke__">new</span>(<span class="hljs-number">0</span>);
    *counter.<span class="hljs-title function_ invoke__">lock</span>().<span class="hljs-keyword">await</span> += <span class="hljs-number">1</span>;
    <span class="hljs-built_in">assert_eq!</span>(*counter.<span class="hljs-title function_ invoke__">lock</span>().<span class="hljs-keyword">await</span>, <span class="hljs-number">1</span>);
&#125;`,!0),c(u),c(s);var d=T(s,12),f=i(d);g(f,()=>`cargo add asyncband --features blocking,oneshot`,!0),c(f),c(d);var p=T(d,2),m=i(p);g(m,()=>`<span class="hljs-keyword">use</span> std::thread;

<span class="hljs-keyword">use</span> asyncband::blocking::FutureExt <span class="hljs-keyword">as</span> _;
<span class="hljs-keyword">use</span> asyncband::oneshot;

<span class="hljs-keyword">let</span> (sender, receiver) = oneshot::<span class="hljs-title function_ invoke__">channel</span>();
thread::<span class="hljs-title function_ invoke__">spawn</span>(<span class="hljs-keyword">move</span> || &#123;
    <span class="hljs-keyword">let</span> <span class="hljs-variable">result</span> = <span class="hljs-number">6</span> * <span class="hljs-number">7</span>;
    sender.<span class="hljs-title function_ invoke__">send</span>(result).<span class="hljs-title function_ invoke__">unwrap</span>();
&#125;);

<span class="hljs-built_in">assert_eq!</span>(receiver.<span class="hljs-title function_ invoke__">block_on</span>(), <span class="hljs-title function_ invoke__">Ok</span>(<span class="hljs-number">42</span>));`,!0),c(m),c(p),l(26),w(e,n)},$$slots:{default:!0}}))}export{W as component};